#ifndef MAIN_H
#define MAIN_H

/*needed library for variadic functions*/
#include <stdarg.h>

int _putchar(const char c);
int _printf(const char *format, ...);
int _print_integer(int n);
int _print_str(char *str):

#endif
